スケジュール内でイベントが重なって表示されてしまうので、
１つのスケジュールにイベントを3つ表示できるよう行を増やす

イベントをD&Dできるようにする

イベントを伸び縮みさせたあとにイベント追加のダイアログが出ないようにする

イベントの右端を掴むと期間を伸び縮みできるようにする
イベントのバツボタンをイベントの左端に移動する

開始週、終了週のインデックス起点を3月第1週=0から、1月第1週=1に変更する
UIもDBデータも


週インデックス(i)と月週表示（m月第n週）の変換処理に重複定義があれば、共通関数化する。
適するライブラリがあれば、利用する

イベント追加ダイアログの開始週の選択肢を13などから「4月第1週」

いま縦軸が野菜、横軸が週だが、逆転させて縦軸を週、横軸を野菜に変更する
横軸の列幅は固定長とする

開始月の入力欄を削除する
タイムラインの月ラベルから年（2026年など）を削除する

スケジュールの列順序を入れ替えられるようにD&Dできるようにする

スケジュールの色付きの帯を、上端を最早のイベントに、下端を最遅のイベントに合わして表示する

テンプレートテーブルに出典カラムを追加
テンプレートに　スケジュールテンプレート.csvを追加する　出典カラム： CSVのソースURLカラム

スケジュールの列ヘッダにあるバツボタンを削除
スケジュールの列ヘッダをクリックすると、ミニウィンドウが出て、色、名前変更欄、削除ボタン、テンプレートに反映ボタン

テンプレートに反映ボタンは、そのスケジュールの野菜名と同名のテンプレートの色、イベントリストを上書き更新する

スケジュールの名前を表示する横幅が足りないときは、名前テキストを折り返す

スケジュールの名前（種まきなど）を表示する横幅が足りないときは、名前テキストを折り返す

スケジュールの列ヘッダから行える色選択は、カラーピッカーではなく、Googleカレンダーと同じ固定色の選択（既存）を使います

テンプレート一覧内のテンプレートアイテムから、野菜名、削除。野菜名があった場所にテンプレート名を移動。

テンプレート一覧のソートに開始時期順を追加する。
開始時期は、「土づくり」「種まき」「育苗」「定植」のいずれかにイベント名が部分一致するイベントの開始時期とする。
それがなければイベント一覧のうち収穫の1個後のイベントの開始週とする。年またぎを考慮する。
それもなければ２月を起点として最初にあるイベントとする。

テンプレート一覧のソートに五十音順を追加する
デフォルトの並び順は開始時期順とする

デフォルトの並び順を、開始時期が近い順とする
開始時期が近い順とは、開始時期が現在週を起点として未来方向に探索して近くにある順。過去方向ではない。

「野菜を選んで空のスケジュールを作成」ボタンを、「空のスケジュールを作成」に変更。
「空のスケジュールを作成」の中の野菜名ボックスから予測候補機能を削除する

スケジュール見出し列右端にある追加ボタン「＋」のラベルを、「＋野菜を追加」に変更

スケジュール見出しをクリックすると出てくるポップアップが現在はタイムラインの左上の固定位置に出現してしまう。クリックされた見出しの左下角にポップアップの左上角が接する位置で出現するようにする。

エクスポートボタンに角丸の青背景をつける

---